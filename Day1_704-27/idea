第一次开始系统化的刷题，发现自己还是有些小问题。首先要是二分的条件要把握清楚，建议一直使用左闭右闭，
其次是刷题速度还不够，准备多做一些练习题之后再接下来的时间里逐渐提升速度。
还有就是总结今天的知识点：
704.二分查找
思路：
1.初始化左指针left为0，右指针right为数组长度减一。
2.当left小于等于right时，计算中间位置mid。
3.如果中间位置的值等于目标值，则返回mid。
4.如果中间位置的值大于目标值，则将右指针right移动到mid-1的位置。
5.如果中间位置的值小于目标值，则将左指针left移动到mid+1的位置。
6.重复上述步骤直到找到目标值，或者left大于right。
35.搜索插入位置


27.移除元素
暴力解法
思路：
遍历数组中的每个元素。
当遇到等于指定值的元素时，从当前位置开始，将后续的元素向前移动一位。
每次移动时数组的长度减一，同时调整当前索引以重新检查移动后的当前位置。

双指针法（快慢指针）
思路：

使用两个指针：一个快指针（fastIndex）和一个慢指针（slowIndex）。
快指针遍历数组，慢指针指向更新数组下标的位置。
当快指针指向的元素不等于指定值时，将其复制到慢指针的位置，然后慢指针向前移动一位。
这样可以保证慢指针前面的所有元素都不是指定值。

